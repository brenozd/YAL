def precision 9;
def round 0;

_init
{
    outl 'Loop testing';
    let variavelA;
    let step;

    out 'vA: '; in variavelA;
    out 'step: '; in step;

    outl '';
    while(variavelA > 0)
    {
        out 'vA: '; outl variavelA;
        variavelA -= step;
    }
}